# plugins/

Claude Code channel plugins and MCP servers.

| Plugin | Type | Phase | Purpose |
|---|---|---|---|
| `m365-mail/` | Claude Code Channel plugin (`defineChannel`) | v1.1 | Inbound M365 email → agent session |
| `portal-bridge/` | MCP server | v1.1 | Azure Portal ↔ agent sessions |
| `teams-meeting/` | Claude Code Channel plugin (`defineChannel`) | v1.1 | Teams meeting bot — CoS as silent co-pilot |

All plugins are v1.1 features — not required for Alpha/Beta/v1.0.
See Phase 6 and Phase 8 of the build plan.
